
alias HeadsUp.Incidents.Incident
alias HeadsUp.Repo
alias HeadsUp.Incidents

import Ecto.Query

# Incident
# |> last(:id)
# |> Repo.one()
# |> IO.inspect



Incidents.filter_incidents() |> IO.inspect
